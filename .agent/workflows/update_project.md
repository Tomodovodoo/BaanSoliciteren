---
description: Update the project by fetching emails, organizing them, and updating stats.
---

1. Run `fetch_emails.py`
   // turbo
2. Run `organize_emails.py`
   // turbo
3. Run `update_stats.py`
4. Check `Email/Ongoing` for new emails and update `relevant_info.json` and `stats.json` manually if needed.
